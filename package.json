{
  "name": "queuectl",
  "version": "1.0.0",
  "description": "A persistent background job queue with CLI interface",
  "main": "src/index.js",
  "bin": {
    "queuectl": "./bin/queuectl.js"
  },
  "scripts": {
    "test": "node --test tests/**/*.test.js"
  },
  "keywords": ["job-queue", "cli", "sqlite", "background-jobs"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "commander": "^12.0.0",
    "uuid": "^10.0.0"
  }
}
